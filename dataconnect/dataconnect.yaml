specVersion: "v1beta"
serviceId: "project-manager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-manager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
